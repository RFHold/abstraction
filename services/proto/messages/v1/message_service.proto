syntax = "proto3";
package messages.v1;

message MessageFrame {
  string id = 1;
  string content = 2;
  string date = 3;
}

service MessageService {
  rpc StreamMessages (stream MessageFrame) returns (stream MessageFrame);
}